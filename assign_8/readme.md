# Assignment 8 - PCA Analysis

## Overview
This assignment performs Principal Component Analysis (PCA) on a dataset to reduce dimensionality and visualize the data in lower‑dimensional space.

## Included Files
- `assignment8_pca.ipynb` – Jupyter notebook that loads the dataset, applies PCA, and visualizes the results.
- `Dataset.csv` – The raw data used for the analysis.

## How to Run
1. Install required packages (e.g., `pip install -r requirements.txt`).
2. Open `assignment8_pca.ipynb` in Jupyter Notebook and run all cells.
3. Examine the explained variance plot and the 2‑D/3‑D scatter visualizations.

---
*Automatically generated README.*
