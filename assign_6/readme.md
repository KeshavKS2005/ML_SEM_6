# Assignment 6 - Breast Cancer Diagnosis with WDBC Dataset

## Overview
This assignment explores classification techniques on the Wisconsin Diagnostic Breast Cancer (WDBC) dataset. It includes data preprocessing, model training, and evaluation.

## Included Files
- `assignment_6.ipynb` – Jupyter notebook containing the full analysis pipeline.
- `wdbc.data` – Raw data file for the WDBC dataset.
- `wdbc.names` – Feature names and description for the dataset.

## How to Run
1. Install required packages (e.g., `pip install -r requirements.txt`).
2. Open `assignment_6.ipynb` in Jupyter Notebook and run all cells.
3. Review the generated performance metrics and visualizations.

---
*Automatically generated README.*
